diff --git a/node_modules/@revenuecat/purchases-js/dist/Purchases.es.js b/node_modules/@revenuecat/purchases-js/dist/Purchases.es.js
index 2e7bfd9..12c6836 100644
--- a/node_modules/@revenuecat/purchases-js/dist/Purchases.es.js
+++ b/node_modules/@revenuecat/purchases-js/dist/Purchases.es.js
@@ -20751,14 +20751,16 @@ function iE({
   locale: e,
   customerEmail: t,
   displayMode: n = "overlay",
-  theme: a = "light"
+  theme: a = "light",
+  discountCode: discountCodeValue = null,
+  discountId: discountIdValue = null
 }) {
   const i = {
     theme: a,
     variant: "one-page",
     locale: e,
     allowLogout: !1,
-    showAddDiscounts: !1,
+    showAddDiscounts: !0,
     showAddTaxId: !1,
     allowDiscountRemoval: !1
   }, s = n === "inline" ? {
@@ -20774,7 +20776,9 @@ function iE({
   return {
     transactionId: r,
     settings: s,
-    ...t && { customer: { email: t } }
+    ...t && { customer: { email: t } },
+    ...discountCodeValue && { discountCode: discountCodeValue },
+    ...discountIdValue && { discountId: discountIdValue }
   };
 }
 class sE {
@@ -20902,8 +20906,13 @@ class sE {
         eventCallback: async (h) => {
           const v = h.name, w = h.data;
           try {
-            if (v === Ia.CHECKOUT_LOADED)
+            if (v === Ia.CHECKOUT_LOADED) {
               n(), f(w);
+              (i.discountCode || i.discountId) && d.Checkout.updateCheckout({
+                ...i.discountCode && { discountCode: i.discountCode },
+                ...i.discountId && { discountId: i.discountId }
+              });
+            }
             else if (v === Ia.CHECKOUT_UPDATED)
               f(w);
             else if (v === Ia.CHECKOUT_COMPLETED) {
@@ -20931,7 +20940,9 @@ class sE {
         locale: _,
         customerEmail: p,
         displayMode: s,
-        theme: l
+        theme: l,
+        discountCode: i.discountCode,
+        discountId: i.discountId
       });
       try {
         d.Checkout.open(b);
@@ -21589,7 +21600,9 @@ function TE(r, e) {
           appUserId: e.appUserId,
           presentedOfferingIdentifier: e.productDetails.presentedOfferingContext.offeringIdentifier,
           customerEmail: e.customerEmail,
-          locale: e.selectedLocale || e.defaultLocale
+          locale: e.selectedLocale || e.defaultLocale,
+          discountCode: e.discountCode,
+          discountId: e.discountId
         },
         ...o(g) && {
           displayMode: "inline",
@@ -25704,6 +25717,8 @@ const Pr = class Pr {
       rcPackage: t,
       purchaseOption: n,
       customerEmail: a,
+      discountCode,
+      discountId,
       attributionMetadata: i,
       workflowPurchaseContext: s,
       selectedLocale: l = Or,
@@ -25764,6 +25779,8 @@ const Pr = class Pr {
           appUserId: _,
           purchaseOption: f,
           customerEmail: a,
+          discountCode,
+          discountId,
           attributionMetadata: i,
           workflowPurchaseContext: s,
           metadata: k,
